import React from 'react';
// Здесь будет интеграция с mapbox-gl или другим картографическим сервисом

const EventsMap = ({ events, selectedId, onSelect }) => {
  return (
    <div className="events-map-placeholder">
      {/* Map with event markers will be here. */}
      <p>Map with event markers will be here.</p>
    </div>
  );
};

export default EventsMap; 